// Package unformatted gives the case a positive key: the pinned gofumpt does
// want to change this file, so a golden of zero keys can only mean the case
// stopped running.
package unformatted

func f() {

	x := 1
	println(x)
}
