package blanks

// An unused import is a *deletion*, which anchors at the first deleted line —
// the arm that was already right, kept here so the case measures both.

import (
	"fmt"
	"os"
)

var _ = fmt.Sprint
