version: "2"
linters:
  default: none
  enable:
    - forbidigo
  settings:
    forbidigo:
      forbid:
        - pattern: ^fmt\.Print.*$
          msg: Do not commit print statements.
        - pattern: \bioutil\b(# Use io and os instead)?
      exclude-godoc-examples: true
      analyze-types: false
