version: "2"
linters:
  settings:
    testifylint:
      disable-all: true
      enable:
        - bool-compare
        - empty
        - expected-actual
      bool-compare:
        ignore-custom-types: true
      expected-actual:
        pattern: "^wanted$"
