version: "2"
linters:
  default: none
  enable:
    - iface
  settings:
    iface:
      enable:
        - identical
        - unused
      settings:
        unused:
          exclude:
            - example.com/skip
