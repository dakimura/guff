package session_test

// Present for the same reason as vault's: it puts session in
// paths_with_external_test_package, so the seed compiles session [session.test]
// and the two test edges meet as a cycle.
