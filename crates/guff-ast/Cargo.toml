[package]
name = "guff-ast"
description = "A Rust port of Go's go/token, go/scanner, and go/ast packages."
keywords = ["go", "ast", "scanner", "port"]
categories = ["parser-implementations", "development-tools"]
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "guff"
path = "src/lib.rs"

[dependencies]
rustc-hash = "2"
