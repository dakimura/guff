// Deprecated: The database-backed peerstore will be removed in the future.
package dep

// The package doc lives here, in a file named neither `doc.go` nor `dep.go`.
// Go puts it on whichever file carries the comment above `package`, and
// go-libp2p's `pstoreds` does exactly this.
