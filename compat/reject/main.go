// Package reject is the tiniest package both tools can be pointed at. The cases
// here are about configs neither tool will get as far as linting, so the code
// only has to compile.
package reject

func F() {}
