module guff.dev/compat/oracles/gourl

// Pinned to golangci-lint v2.12.2's own go directive: godebug defaults are
// derived from it, so this is what decides stdlib behaviour guff must match.
// See ../README.md.
go 1.25.0
