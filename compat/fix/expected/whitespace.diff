# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh whitespace
# Produced by running `golangci-lint run --fix` on compat/golden/cases/whitespace/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/bad.go
+++ b/bad.go
@@ -4,18 +4,17 @@
 // multi-line one needs `multi-if` / `multi-func` turned on.
 
 func LeadingNewline() {
-
 	_ = 1
 }
 
 func TrailingNewline() {
 	_ = 1
-
 }
 
 func MultiLineIf(a, b bool) {
 	if a &&
 		b {
+
 		_ = 1
 	}
 }
