# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh nolint-strict
# Produced by running `golangci-lint run --fix` on compat/golden/cases/nolint-strict/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/gap/gap.go
+++ b/gap/gap.go
@@ -1,5 +1,3 @@
-//nolint:errcheck
-
 // A blank line between the directive and the package clause: the comment group
 // ends two lines above `package`, so `r.To == nodeStartLine-1` is false and the
 // file is *not* covered.
--- a/group/group.go
+++ b/group/group.go
@@ -12,6 +12,6 @@
 	// Inside the function body: still covered, we recieve that too.
 }
 
-// B is documented here, in a comment group of its own: this recieve is
+// B is documented here, in a comment group of its own: this receive is
 // reported.
 func B() {}
--- a/inline/inline.go
+++ b/inline/inline.go
@@ -30,7 +30,7 @@
 func LeadingSpace() {
 	// `strings.TrimLeft(text, "/ ")` in the filter means this still suppresses,
 	// while nolintlint reports it as not machine-readable.
-	mkerr() // nolint:errcheck
+	mkerr() //nolint:errcheck
 }
 
 func UpperCase() {
@@ -57,6 +57,6 @@
 
 func IneffectualDirectiveOnTheWrongLine() int {
 	y := 1
-	y = 2 //nolint:ineffassign
+	y = 2
 	return y
 }
--- a/preceding/preceding.go
+++ b/preceding/preceding.go
@@ -23,12 +23,11 @@
 }
 
 func ColumnMismatch() {
-//nolint:errcheck
+
 	mkerr()
 }
 
 func BlankLineGap() {
-	//nolint:errcheck
 
 	mkerr()
 }
--- a/unused/unused.go
+++ b/unused/unused.go
@@ -8,7 +8,7 @@
 package unused
 
 func Specific() {
-	//nolint:errcheck // reported: errcheck is enabled and suppressed nothing
+
 	_ = 1
 }
 
@@ -29,12 +29,12 @@
 }
 
 func Bare() {
-	//nolint // reported without a linter name
+
 	_ = 1
 }
 
 func All() {
-	//nolint:all // reported without a linter name, like the bare form
+
 	_ = 1
 }
 
