# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh modernize
# Produced by running `golangci-lint run --fix` on compat/golden/cases/modernize/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/bloop/bloop.go
+++ b/bloop/bloop.go
@@ -90,7 +90,7 @@
 		wg.Done()
 	}
 	wg.Add(2)
-	for i := 0; i < 2; i++ {
+	for range 2 {
 		go poster()
 	}
 	wg.Wait()
--- a/fmtappendf/fmtappendf.go
+++ b/fmtappendf/fmtappendf.go
@@ -12,10 +12,10 @@
 // The conversion has to be to `[]byte` itself. `types.Identical` is what
 // upstream asks, so a *named* byte slice keeps its own methods and its own
 // nil-vs-empty contract and is left alone; an alias still qualifies.
-func plain(x int) []byte          { return []byte(fmt.Sprintf("%d", x)) }
+func plain(x int) []byte          { return fmt.Appendf(nil, "%d", x) }
 func named(x int) json.RawMessage { return json.RawMessage(fmt.Sprintf("%d", x)) }
 func localNamed(x int) myBytes    { return myBytes(fmt.Sprintf("%d", x)) }
-func alias(x int) aliasBytes      { return aliasBytes(fmt.Sprintf("%d", x)) }
+func alias(x int) aliasBytes      { return fmt.Appendf(nil, "%d", x) }
 func toString(x int) string       { return string(fmt.Sprintf("%d", x)) }
 
 // `[]byte(fmt.Sprintf(""))` is an empty but non-nil slice while
@@ -24,39 +24,39 @@
 // one of `s v x X`. Any other verb, or one byte of literal text, and the
 // rewrite is offered. `Sprintln` always writes a newline and is never skipped.
 func fEmpty() []byte               { return []byte(fmt.Sprintf("")) }
-func fD(x int) []byte              { return []byte(fmt.Sprintf("%d", x)) }
+func fD(x int) []byte              { return fmt.Appendf(nil, "%d", x) }
 func fS(x string) []byte           { return []byte(fmt.Sprintf("%s", x)) }
 func fV(x any) []byte              { return []byte(fmt.Sprintf("%v", x)) }
 func fLowerX(x string) []byte      { return []byte(fmt.Sprintf("%x", x)) }
 func fUpperX(x string) []byte      { return []byte(fmt.Sprintf("%X", x)) }
-func fQ(x string) []byte           { return []byte(fmt.Sprintf("%q", x)) }
-func fLeadingText(x string) []byte { return []byte(fmt.Sprintf("a%s", x)) }
+func fQ(x string) []byte           { return fmt.Appendf(nil, "%q", x) }
+func fLeadingText(x string) []byte { return fmt.Appendf(nil, "a%s", x) }
 func fTwoS(x, y string) []byte     { return []byte(fmt.Sprintf("%s%s", x, y)) }
 func fPrecS(x string) []byte       { return []byte(fmt.Sprintf("%.5s", x)) }
-func fPrecD(x int) []byte          { return []byte(fmt.Sprintf("%.5d", x)) }
+func fPrecD(x int) []byte          { return fmt.Appendf(nil, "%.5d", x) }
 func fWidthS(x string) []byte      { return []byte(fmt.Sprintf("%10s", x)) }
-func fNoVerb() []byte              { return []byte(fmt.Sprintf("plain")) }
-func fPercent(x string) []byte     { return []byte(fmt.Sprintf("%%%s", x)) }
+func fNoVerb() []byte              { return fmt.Appendf(nil, "plain") }
+func fPercent(x string) []byte     { return fmt.Appendf(nil, "%%%s", x) }
 func fIndexed(x string) []byte     { return []byte(fmt.Sprintf("%[1]s", x)) }
-func fMixed(x int) []byte          { return []byte(fmt.Sprintf("%v%d", x, x)) }
+func fMixed(x int) []byte          { return fmt.Appendf(nil, "%v%d", x, x) }
 
 // `Sprint` has no format string of its own; the guard only bites when the first
 // argument is a constant.
-func sprintVar(x int) []byte   { return []byte(fmt.Sprint(x)) }
+func sprintVar(x int) []byte   { return fmt.Append(nil, x) }
 func sprintEmpty() []byte      { return []byte(fmt.Sprint("")) }
 func sprintNoArgs() []byte     { return []byte(fmt.Sprint()) }
-func sprintlnVar(x int) []byte { return []byte(fmt.Sprintln(x)) }
+func sprintlnVar(x int) []byte { return fmt.Appendln(nil, x) }
 
 // The rendering of the fix must not decide the diagnostic. `expr_text` renders
 // a call only when it has exactly one argument, so a `fmt.Sprintf` whose
 // operand is a no-argument method call came back `None` and the *finding* went
 // with it. Upstream splices source ranges and has no rendering that can fail.
 func appendfCallOperand(w writer, r request) {
-	w.Write([]byte(fmt.Sprintf("unexpected UA: %s", r.UserAgent())))
+	w.Write(fmt.Appendf(nil, "unexpected UA: %s", r.UserAgent()))
 }
 
 func appendfTwoArgCallOperand(w writer, r request) {
-	w.Write([]byte(fmt.Sprintf("%s %s", r.Header("a", "b"), r.UserAgent())))
+	w.Write(fmt.Appendf(nil, "%s %s", r.Header("a", "b"), r.UserAgent()))
 }
 
 type writer interface{ Write(p []byte) (int, error) }
--- a/mapsloop/mapsloop.go
+++ b/mapsloop/mapsloop.go
@@ -2,10 +2,10 @@
 
 package modernize
 
+import "maps"
+
 func copyMap(dst, src map[int]string) {
-	for k, v := range src {
-		dst[k] = v
-	}
+	maps.Copy(dst, src)
 }
 
 // The destination is a *call result*, indexed. syncthing `lib/httpcache` writes
@@ -18,7 +18,5 @@
 type recorder struct{ header headers }
 
 func (r *recorder) writeHeader(w interface{ Header() headers }) {
-	for k, v := range r.header {
-		w.Header()[k] = v
-	}
+	maps.Copy(w.Header(), r.header)
 }
--- a/minmax/minmax.go
+++ b/minmax/minmax.go
@@ -4,19 +4,13 @@
 // `lhs0 = rhs0` immediately above `if a < b { lhs = rhs }` with no else, and it
 // says "if statement", where pattern 1 says "if/else statement".
 func clampMin(x, y int) int {
-	v := x
-	if v > y {
-		v = y
-	}
+	v := min(x, y)
 
 	return v
 }
 
 func clampMax(x, y int) int {
-	v := x
-	if v < y {
-		v = y
-	}
+	v := max(x, y)
 
 	return v
 }
@@ -25,10 +19,7 @@
 func clampPlainAssign(x, y int) int {
 	var v int
 
-	v = x
-	if v > y {
-		v = y
-	}
+	v = min(x, y)
 
 	return v
 }
@@ -68,11 +59,7 @@
 func withElse(x, y int) int {
 	var v int
 
-	if x > y {
-		v = y
-	} else {
-		v = x
-	}
+	v = min(x, y)
 
 	return v
 }
@@ -81,10 +68,7 @@
 // name — and not "the same value", so `len(a)` matches `len(a)` although a call
 // is not provably pure. syncthing writes this five times.
 func clampToLen(a, b []string) int {
-	count := len(a)
-	if len(b) < len(a) {
-		count = len(b)
-	}
+	count := min(len(b), len(a))
 
 	return count
 }
@@ -92,10 +76,7 @@
 // And the two spellings of one expression differ only in spacing, which is not
 // part of the syntax.
 func clampArith(buf []byte, written, chunk int) int {
-	toWrite := chunk
-	if toWrite > len(buf)-written {
-		toWrite = len(buf) - written
-	}
+	toWrite := min(chunk, len(buf)-written)
 
 	return toWrite
 }
@@ -106,10 +87,7 @@
 // fix. syncthing `internal/db/sqlite/folderdb_update.go` writes this with a
 // function literal inside the call, which no hand-written printer covers.
 func clampIndexFromCall(es []int) int {
-	globIdx := slicesIndexFunc(es, func(e int) bool { return e > 0 })
-	if globIdx < 0 {
-		globIdx = 0
-	}
+	globIdx := max(slicesIndexFunc(es, func(e int) bool { return e > 0 }), 0)
 
 	return es[globIdx]
 }
--- a/minmaxfloat/minmaxfloat.go
+++ b/minmaxfloat/minmaxfloat.go
@@ -13,15 +13,6 @@
 	return b
 }
 
-// Reported: the comparison's operands are returned in the *other* order, which
-// flips the sign back to `max`.
-func max(a, b int) int {
-	if b > a {
-		return b
-	}
-	return a
-}
-
 var (
 	_ = min(1.0, 2.0)
 	_ = max(1, 2)
--- a/minmaxshadow/minmaxshadow.go
+++ b/minmaxshadow/minmaxshadow.go
@@ -14,15 +14,6 @@
 // A local shadow counts too (upstream's own testdata has `hour, min := 3600,
 // 60`), which is why the lookup starts at the innermost scope.
 package minmax
-
-// This declaration is itself a finding — the other arm of the rule — and it is
-// what shadows `min` for everything below.
-func min(a, b int) int {
-	if a < b {
-		return a
-	}
-	return b
-}
 
 // silent: `min` is this package's function, not the builtin.
 func shadowedIf(a, b int) int {
@@ -46,21 +37,14 @@
 
 // FINDING: nothing shadows `max`.
 func maxIf(a, b int) int {
-	x := a
-	if x < b {
-		x = b
-	}
+	x := max(a, b)
 	return x
 }
 
 // FINDING: the if/else arm of `max`.
 func maxIfElse(a, b int) int {
 	var x int
-	if a < b {
-		x = b
-	} else {
-		x = a
-	}
+	x = max(a, b)
 	return x
 }
 
--- a/minmaxuser/minmaxuser.go
+++ b/minmaxuser/minmaxuser.go
@@ -12,23 +12,6 @@
 // expressions to be the comparison's own operands, in either order, with the
 // direction spelling the function's name.
 package minmax
-
-// Pattern 1: one if/else.
-func min(a, b int) int {
-	if a < b {
-		return a
-	} else {
-		return b
-	}
-}
-
-// Pattern 2: an if, then a return.
-func max(a, b int64) int64 {
-	if a > b {
-		return a
-	}
-	return b
-}
 
 type holder struct{}
 
--- a/plusbuild/plusbuild.go
+++ b/plusbuild/plusbuild.go
@@ -3,7 +3,6 @@
 // golden records the same finding wherever it is regenerated.
 
 //go:build go1.18
-// +build go1.18
 
 package plusbuild
 
--- a/reflecttypefor/reflecttypefor.go
+++ b/reflecttypefor/reflecttypefor.go
@@ -9,12 +9,11 @@
 }
 
 func typeofVar() reflect.Type {
-	var zero MyStruct
-	return reflect.TypeOf(zero)
+	return reflect.TypeFor[MyStruct]()
 }
 
 func typeofElem() reflect.Type {
-	return reflect.TypeOf((*MyStruct)(nil)).Elem()
+	return reflect.TypeFor[MyStruct]()
 }
 
 // Interface-typed args are dynamic; must not suggest TypeFor.
@@ -35,9 +34,9 @@
 // writes eleven of these in a row.
 func starNew() []reflect.Type {
 	return []reflect.Type{
-		reflect.TypeOf(*new(string)),
-		reflect.TypeOf(*new(int64)),
-		reflect.TypeOf(*new([]byte)),
+		reflect.TypeFor[string](),
+		reflect.TypeFor[int64](),
+		reflect.TypeFor[[]byte](),
 	}
 }
 
@@ -46,19 +45,19 @@
 // println or recover.
 func pureBuiltins(s []int) []reflect.Type {
 	return []reflect.Type{
-		reflect.TypeOf(len(s)),
-		reflect.TypeOf(make([]int, 3)),
-		reflect.TypeOf(min(1, 2)),
+		reflect.TypeFor[int](),
+		reflect.TypeFor[[]int](),
+		reflect.TypeFor[int](),
 	}
 }
 
 // Operand shapes `NoEffects` accepts that an outermost-node-only check missed.
 func operandShapes(a, b int, s []int, x any, m map[string]int, k string) []reflect.Type {
 	return []reflect.Type{
-		reflect.TypeOf(a + b),
-		reflect.TypeOf(s[1:2]),
-		reflect.TypeOf(x.(MyStruct)),
-		reflect.TypeOf(m[k]),
+		reflect.TypeFor[int](),
+		reflect.TypeFor[[]int](),
+		reflect.TypeFor[MyStruct](),
+		reflect.TypeFor[int](),
 	}
 }
 
@@ -77,13 +76,13 @@
 func elemTooLong(p *AVeryLongTypeNameIndeed, q *MyStruct) []reflect.Type {
 	return []reflect.Type{
 		reflect.TypeOf(p).Elem(),
-		reflect.TypeOf(q).Elem(),
+		reflect.TypeFor[MyStruct](),
 	}
 }
 
 // `TypeOf((*T)(nil))` standing alone, with no `.Elem()` after it.
 func nilConversionWithoutElem() reflect.Type {
-	return reflect.TypeOf((*MyStruct)(nil))
+	return reflect.TypeFor[*MyStruct]()
 }
 
 type List[T any] struct{ items []T }
@@ -94,9 +93,9 @@
 // is not "complicated" the way an unnamed `interface{}` would be.
 func nilConversions() []reflect.Type {
 	return []reflect.Type{
-		reflect.TypeOf(map[string]any(nil)),
-		reflect.TypeOf([]int(nil)),
-		reflect.TypeOf(map[string]int(nil)),
+		reflect.TypeFor[map[string]any](),
+		reflect.TypeFor[[]int](),
+		reflect.TypeFor[map[string]int](),
 	}
 }
 
@@ -104,14 +103,12 @@
 // arguments, so a named generic instantiated with an unnamed struct or an
 // unnamed interface is still complicated.
 func generics() []reflect.Type {
-	var a List[int]
 	var b List[struct{ N int }]
-	var c List[Expr]
 	var d List[interface{ Bar() }]
 	return []reflect.Type{
-		reflect.TypeOf(a),
+		reflect.TypeFor[List[int]](),
 		reflect.TypeOf(b),
-		reflect.TypeOf(c),
+		reflect.TypeFor[List[Expr]](),
 		reflect.TypeOf(d),
 	}
 }
--- a/slicesbackward/slicesbackward.go
+++ b/slicesbackward/slicesbackward.go
@@ -2,10 +2,12 @@
 
 package modernize
 
+import "slices"
+
 func sumBackward(s []int) int {
 	sum := 0
-	for i := len(s) - 1; i >= 0; i-- {
-		sum += s[i]
+	for _, v := range slices.Backward(s) {
+		sum += v
 	}
 	return sum
 }
--- a/slicescontains/slicescontains.go
+++ b/slicescontains/slicescontains.go
@@ -1,50 +1,28 @@
 package slicescontains
 
+import "slices"
+
 func containsReturn(s []int, needle int) bool {
-	for _, v := range s {
-		if v == needle {
-			return true
-		}
-	}
-	return false
+	return slices.Contains(s, needle)
 }
 
 func containsIndexElem(s []int, needle int) bool {
-	for i := range s {
-		if s[i] == needle {
-			return true
-		}
-	}
-	return false
+	return slices.Contains(s, needle)
 }
 
 func containsFuncPred(s []int) bool {
 	pred := func(v int) bool { return v > 0 }
-	for _, v := range s {
-		if pred(v) {
-			return true
-		}
-	}
-	return false
+	return slices.ContainsFunc(s, pred)
 }
 
 func containsBreakBody(s []int, needle int) {
-	for _, v := range s {
-		if v == needle {
-			println("found")
-			break
-		}
+	if slices.Contains(s, needle) {
+		println("found")
 	}
 }
 
 func containsFoundAssign(s []int, needle int) bool {
-	found := false
-	for _, v := range s {
-		if v == needle {
-			found = true
-			break
-		}
-	}
+	found := slices.Contains(s, needle)
 	return found
 }
 
@@ -122,46 +100,26 @@
 
 // Reported: the parameter type is the element type.
 func containsFuncIdenticalParam(stack []*obj) bool {
-	for _, vis := range stack {
-		if objPred(vis) {
-			return true
-		}
-	}
-	return false
+	return slices.ContainsFunc(stack, objPred)
 }
 
 // Reported: the element type *is* the interface.
 func containsFuncIfaceElem(stack []value) bool {
-	for _, vis := range stack {
-		if ifacePred(vis) {
-			return true
-		}
-	}
-	return false
+	return slices.ContainsFunc(stack, ifacePred)
 }
 
 // Upstream's only test on the needle is `usesRangeVar`. There is no purity
 // check: `slices.Contains(s, strings.ToLower(k))` evaluates the needle once
 // where the loop evaluated it per element, and upstream rewrites it anyway.
 func containsCallNeedle(hide []string, key string) bool {
-	for _, k := range hide {
-		if lower(key) == k {
-			return true
-		}
-	}
-	return false
+	return slices.Contains(hide, lower(key))
 }
 
 type hider struct{ hide []string }
 
 // The same with the slice behind a selector, which is how beats writes it.
 func (h *hider) containsCallNeedleField(key string) bool {
-	for _, k := range h.hide {
-		if lower(key) == k {
-			return true
-		}
-	}
-	return false
+	return slices.Contains(h.hide, lower(key))
 }
 
 // Silent: the needle reads the range variable.
--- a/stditerators/stditerators.go
+++ b/stditerators/stditerators.go
@@ -6,22 +6,22 @@
 
 // C-style loop over Struct.{NumFields,Field} -> Struct.Fields iteration.
 func useStruct(s *types.Struct) {
-	for i := 0; i < s.NumFields(); i++ {
-		_ = s.Field(i)
+	for field := range s.Fields() {
+		_ = field
 	}
 }
 
 // range-over-int form: for i := range s.NumFields().
 func useStructRange(s *types.Struct) {
-	for i := range s.NumFields() {
-		_ = s.Field(i)
+	for field := range s.Fields() {
+		_ = field
 	}
 }
 
 // Tuple.{Len,At} -> Tuple.Variables iteration.
 func useTuple(t *types.Tuple) {
-	for i := 0; i < t.Len(); i++ {
-		_ = t.At(i)
+	for v := range t.Variables() {
+		_ = v
 	}
 }
 
@@ -37,7 +37,7 @@
 
 // Not modernizable: no matching table type.
 func plainSlice(xs []int) {
-	for i := 0; i < len(xs); i++ {
+	for i := range xs {
 		_ = xs[i]
 	}
 }
--- a/stringsbuilder/stringsbuilder.go
+++ b/stringsbuilder/stringsbuilder.go
@@ -1,43 +1,46 @@
 package modernize
+
+import "strings"
 
 // basic: var + loop += + rvalue use
 func builderBasic() {
-	var s string
-	s += "before"
-	for i := 0; i < 10; i++ {
-		s += "in"
+	var s strings.Builder
+	s.WriteString("before")
+	for range 10 {
+		s.WriteString("in")
 	}
-	s += "after"
-	print(s)
+	s.WriteString("after")
+	print(s.String())
 }
 
 // short decl
 func builderShort() {
-	s := "a"
-	for i := 0; i < 10; i++ {
-		s += "b"
+	var s strings.Builder
+	s.WriteString("a")
+	for range 10 {
+		s.WriteString("b")
 	}
-	print(s)
+	print(s.String())
 }
 
 // empty short decl
 func builderEmptyShort() {
-	s := ""
-	for i := 0; i < 10; i++ {
-		s += "b"
+	var s strings.Builder
+	for range 10 {
+		s.WriteString("b")
 	}
-	print(s)
+	print(s.String())
 }
 
 // paren var decl where s is last
 func builderParenLast(slice []string) string {
 	var (
-		msg string
+		msg strings.Builder
 	)
 	for _, s := range slice {
-		msg += s
+		msg.WriteString(s)
 	}
-	return msg
+	return msg.String()
 }
 
 // nope: += only outside a loop
@@ -51,7 +54,7 @@
 // nope: declaration in if init (not an unrestricted stmt list)
 func builderIfInit() {
 	if s := "a"; true {
-		for i := 0; i < 10; i++ {
+		for range 10 {
 			s += "x"
 		}
 		print(s)
@@ -62,7 +65,7 @@
 func builderDirectAssign(x string) string {
 	var s string
 	s = x
-	for i := 0; i < 3; i++ {
+	for range 3 {
 		s += x
 	}
 	return s
@@ -74,7 +77,7 @@
 		str   = "hello"
 		after int
 	)
-	for i := 0; i < 100; i++ {
+	for range 100 {
 		str += "!"
 	}
 	println(str, after)
--- a/stringscutprefix/stringscutprefix.go
+++ b/stringscutprefix/stringscutprefix.go
@@ -6,29 +6,29 @@
 )
 
 func pattern2TrimPrefix(s, pre string) string {
-	if after := strings.TrimPrefix(s, pre); after != s {
+	if after, ok := strings.CutPrefix(s, pre); ok {
 		return after
 	}
 	return s
 }
 
 func pattern2TrimSuffix(s, suf string) string {
-	if before := strings.TrimSuffix(s, suf); before != s {
+	if before, ok := strings.CutSuffix(s, suf); ok {
 		return before
 	}
 	return s
 }
 
 func bytesHasPrefix(b, pre []byte) []byte {
-	if bytes.HasPrefix(b, pre) {
-		return bytes.TrimPrefix(b, pre)
+	if after, ok := bytes.CutPrefix(b, pre); ok {
+		return after
 	}
 	return b
 }
 
 func bytesHasSuffix(b, suf []byte) []byte {
-	if bytes.HasSuffix(b, suf) {
-		return bytes.TrimSuffix(b, suf)
+	if before, ok := bytes.CutSuffix(b, suf); ok {
+		return before
 	}
 	return b
 }
--- a/stringsseq/stringsseq.go
+++ b/stringsseq/stringsseq.go
@@ -8,13 +8,13 @@
 )
 
 func rangeSplit(s string) {
-	for _, part := range strings.Split(s, ",") {
+	for part := range strings.SplitSeq(s, ",") {
 		_ = part
 	}
 }
 
 func rangeFields(s string) {
-	for _, part := range strings.Fields(s) {
+	for part := range strings.FieldsSeq(s) {
 		_ = part
 	}
 }
@@ -24,13 +24,13 @@
 // release, and the analyzer's doc comment lists them. syncthing
 // `cmd/syncthing/crash_reporting.go` ranges over `bytes.Split(data, …)`.
 func rangeBytesSplit(data []byte) {
-	for _, line := range bytes.Split(data, []byte("\n")) {
+	for line := range bytes.SplitSeq(data, []byte("\n")) {
 		_ = line
 	}
 }
 
 func rangeBytesFields(data []byte) {
-	for _, f := range bytes.Fields(data) {
+	for f := range bytes.FieldsSeq(data) {
 		_ = f
 	}
 }
--- a/testingcontext/testingcontext.go
+++ b/testingcontext/testingcontext.go
@@ -22,16 +22,14 @@
 
 // Reported: the plain test function.
 func TestPlain(t *testing.T) {
-	ctx, cancel := context.WithCancel(context.Background())
-	defer cancel()
+	ctx := t.Context()
 	_ = ctx
 }
 
 // Reported: a `t.Run` subtest — the literal is argument index 1 of `T.Run`.
 func TestSubtest(t *testing.T) {
 	t.Run("x", func(t *testing.T) {
-		ctx, cancel := context.WithCancel(context.Background())
-		defer cancel()
+		ctx := t.Context()
 		_ = ctx
 	})
 }
@@ -92,8 +90,7 @@
 // Reported: `B.Run` counts as well as `T.Run`, and the message names `b`.
 func BenchmarkSub(b *testing.B) {
 	b.Run("x", func(b *testing.B) {
-		ctx, cancel := context.WithCancel(context.Background())
-		defer cancel()
+		ctx := b.Context()
 		_ = ctx
 	})
 }
@@ -109,8 +106,7 @@
 
 // Reported: `context.TODO` is the other accepted parent.
 func TestTODO(t *testing.T) {
-	ctx, cancel := context.WithCancel(context.TODO())
-	defer cancel()
+	ctx := t.Context()
 	_ = ctx
 }
 
@@ -128,8 +124,7 @@
 // Reported: a bare block is still inside the test function.
 func TestNestedBlock(t *testing.T) {
 	{
-		ctx, cancel := context.WithCancel(context.Background())
-		defer cancel()
+		ctx := t.Context()
 		_ = ctx
 	}
 }
@@ -137,8 +132,7 @@
 // Reported: so is a `for` body.
 func TestForBody(t *testing.T) {
 	for range 1 {
-		ctx, cancel := context.WithCancel(context.Background())
-		defer cancel()
+		ctx := t.Context()
 		_ = ctx
 	}
 }
@@ -147,8 +141,7 @@
 func TestSwitchCase(t *testing.T) {
 	switch 1 {
 	case 1:
-		ctx, cancel := context.WithCancel(context.Background())
-		defer cancel()
+		ctx := t.Context()
 		_ = ctx
 	}
 }
@@ -156,8 +149,7 @@
 // Reported: the message names whatever the parameter is called.
 func TestRenamedParam(t *testing.T) {
 	t.Run("x", func(sub *testing.T) {
-		ctx, cancel := context.WithCancel(context.Background())
-		defer cancel()
+		ctx := sub.Context()
 		_ = ctx
 	})
 }
--- a/unsafefuncs/unsafefuncs.go
+++ b/unsafefuncs/unsafefuncs.go
@@ -3,17 +3,17 @@
 import "unsafe"
 
 func addConst(ptr unsafe.Pointer) unsafe.Pointer {
-	return unsafe.Pointer(uintptr(ptr) + 1)
+	return unsafe.Add(ptr, 1)
 }
 
 func addVar(ptr unsafe.Pointer, n int) unsafe.Pointer {
-	return unsafe.Pointer(uintptr(ptr) + uintptr(n))
+	return unsafe.Add(ptr, n)
 }
 
 type uP = unsafe.Pointer
 
 func addAlias(ptr uP) uP {
-	return uP(uintptr(ptr) + 1)
+	return unsafe.Add(ptr, 1)
 }
 
 type namedUP unsafe.Pointer
--- a/waitgroupgo/waitgroupgo.go
+++ b/waitgroupgo/waitgroupgo.go
@@ -5,11 +5,9 @@
 import "sync"
 
 func spawn(wg *sync.WaitGroup) {
-	wg.Add(1)
-	go func() {
-		defer wg.Done()
+	wg.Go(func() {
 		_ = 1
-	}()
+	})
 }
 
 // "Body must start with `defer wg.Done()` or end with `wg.Done()`." guff had
@@ -19,28 +17,22 @@
 type wgHolder struct{ wg sync.WaitGroup }
 
 func spawnTrailingDone(wg *sync.WaitGroup) {
-	wg.Add(1)
-	go func() {
+	wg.Go(func() {
 		work()
-		wg.Done()
-	}()
+	})
 }
 
 // The receiver only has to be the same *syntax* the `Add` used.
 func spawnIndexedReceiver(wgs []sync.WaitGroup) {
-	wgs[0].Add(1)
-	go func() {
+	wgs[0].Go(func() {
 		work()
-		wgs[0].Done()
-	}()
+	})
 }
 
 func spawnFieldReceiver(h *wgHolder) {
-	h.wg.Add(1)
-	go func() {
+	h.wg.Go(func() {
 		work()
-		h.wg.Done()
-	}()
+	})
 }
 
 // Silent: `Done` is neither first-and-deferred nor last.
