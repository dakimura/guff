# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh staticcheck-cgo
# Produced by running `golangci-lint run --fix` on compat/golden/cases/staticcheck-cgo/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/cgo/notcgo.go
+++ b/cgo/notcgo.go
@@ -2,6 +2,6 @@
 package cgo
 
 func handWritten() {
-	var y *int = new(int) // FINDING
+	var y = new(int) // FINDING
 	_ = y
 }
