# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh govet-disable
# Produced by running `golangci-lint run --fix` on compat/golden/cases/govet-disable/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/settings/bad.go
+++ b/settings/bad.go
@@ -24,5 +24,4 @@
 
 func Unreachable() int {
 	return 1
-	return 2 // unreachable
 }
