# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh wsl-v5
# Produced by running `golangci-lint run --fix` on compat/golden/cases/wsl-v5/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/bad/bad.go
+++ b/bad/bad.go
@@ -3,12 +3,15 @@
 func Bad() {
 	one := 1
 	two := 2
+
 	three := 3
 	if three == 3 {
 		_ = one
 		_ = two
+
 		return
 	}
+
 	four := 4
 	_ = four
 }
@@ -80,7 +83,6 @@
 // blank line *is* reported. Silencing everything does not pass this fixture.
 func ErrWithBlankLine(v string) error {
 	_, err := parse(v)
-
 	if err != nil {
 		return err
 	}
@@ -95,7 +97,6 @@
 // inserted newline, and nothing in this fixture exercised them
 // (COMPAT-HARDENING 続き 77).
 func BlockLeadingWSV5() {
-
 	x := 1
 	_ = x
 }
@@ -103,5 +104,4 @@
 func BlockTrailingWSV5() {
 	x := 1
 	_ = x
-
 }
