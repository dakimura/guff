# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh nakedret
# Produced by running `golangci-lint run --fix` on compat/golden/cases/nakedret/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/bad.go
+++ b/bad.go
@@ -2,7 +2,7 @@
 
 func Bad() (n int) {
 	n = 1
-	return
+	return n
 }
 
 // nakedret names the function and the line count, so two functions over the
@@ -11,5 +11,5 @@
 	s = "x"
 	err = nil
 
-	return
+	return s, err
 }
