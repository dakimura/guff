# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh copyloopvar
# Produced by running `golangci-lint run --fix` on compat/golden/cases/copyloopvar/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/bad.go
+++ b/bad.go
@@ -2,8 +2,7 @@
 
 func Bad() {
 	for i, v := range []int{1, 2, 3} {
-		i := i
-		v := v
+
 		_, _ = i, v
 	}
 }
@@ -12,14 +11,14 @@
 // and the `for i := range n` form is a separate node from `range slice`.
 func RangeInt() {
 	for i := range 3 {
-		i := i
+
 		_ = i
 	}
 }
 
 func ThreeClause() {
 	for i := 0; i < 3; i++ {
-		i := i
+
 		_ = i
 	}
 }
