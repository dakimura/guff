# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh staticcheck-sa
# Produced by running `golangci-lint run --fix` on compat/golden/cases/staticcheck-sa/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/sa1006/bad/bad.go
+++ b/sa1006/bad/bad.go
@@ -3,7 +3,7 @@
 import "fmt"
 
 func fn(s string) {
-	fmt.Printf(s)
+	fmt.Print(s)
 }
 
 func main() { fn("x") }
--- a/sa1008/bad/bad.go
+++ b/sa1008/bad/bad.go
@@ -4,7 +4,7 @@
 
 func main() {
 	h := http.Header{}
-	_ = h["foo"]
+	_ = h["Foo"]
 	var r http.Request
-	_ = r.Header["etag"]
+	_ = r.Header["Etag"]
 }
--- a/sa1013/bad/bad.go
+++ b/sa1013/bad/bad.go
@@ -4,5 +4,5 @@
 
 func main() {
 	var s io.Seeker
-	s.Seek(io.SeekStart, 0)
+	s.Seek(0, io.SeekStart)
 }
--- a/sa1018/ok/ok.go
+++ b/sa1018/ok/ok.go
@@ -3,6 +3,6 @@
 import "strings"
 
 func main() {
-	strings.Replace("abc", "a", "b", -1)
+	strings.ReplaceAll("abc", "a", "b")
 	strings.Replace("abc", "a", "b", 1)
 }
--- a/sa1025/bad/bad.go
+++ b/sa1025/bad/bad.go
@@ -5,6 +5,6 @@
 func fn() {
 	t := time.NewTimer(time.Second)
 	if t.Reset(time.Second) {
-		_ = <-t.C
+		<-t.C
 	}
 }
--- a/sa1029/bad/bad.go
+++ b/sa1029/bad/bad.go
@@ -40,12 +40,11 @@
 	// reported both; fiber had one of each.
 	var k2 ctxKey = struct{}{}
 	context.WithValue(ctx, k2, val)
-	var k5 ctxKey
-	k5 = struct{}{}
+	var k5 ctxKey = struct{}{}
 	context.WithValue(ctx, k5, val)
 
 	// The same named type, spelled the ordinary ways.
-	var k1 ctxKey = ctxKey{}
+	var k1 = ctxKey{}
 	context.WithValue(ctx, k1, val)
 	k3 := ctxKey{}
 	context.WithValue(ctx, k3, val)
@@ -56,7 +55,7 @@
 
 	// A named struct with a field, and a named built-in type: neither is a
 	// `*types.Struct` with no fields, and neither is a `*types.Basic`.
-	var k12 ctxKeyField = ctxKeyField{n: 1}
+	var k12 = ctxKeyField{n: 1}
 	context.WithValue(ctx, k12, val)
 	var k11 namedInt = 3
 	context.WithValue(ctx, k11, val)
--- a/sa4013/bad/bad.go
+++ b/sa4013/bad/bad.go
@@ -1,2 +1,3 @@
 package main
+
 func main() { _ = !!true }
--- a/sa4014/bad/bad.go
+++ b/sa4014/bad/bad.go
@@ -1,7 +1,9 @@
 package main
+
 func main() {
-    x := 1
-    if x == 1 {
-    } else if x == 1 {
-    }
+	x := 1
+	switch x {
+	case 1:
+	case 1:
+	}
 }
--- a/sa4014/ok/ok.go
+++ b/sa4014/ok/ok.go
@@ -1,7 +1,9 @@
 package main
+
 func main() {
-    x := 1
-    if x == 1 {
-    } else if x == 2 {
-    }
+	x := 1
+	switch x {
+	case 1:
+	case 2:
+	}
 }
--- a/sa4023/bad/bad.go
+++ b/sa4023/bad/bad.go
@@ -2,7 +2,6 @@
 
 func main() {
 	var p *int
-	var i any
-	i = p
+	var i any = p
 	_ = i == nil
 }
--- a/sa4026/bad/bad.go
+++ b/sa4026/bad/bad.go
@@ -1,2 +1,3 @@
 package main
-func main() { _ = -0.0 }
+
+func main() { _ = math.Copysign(0, -1) }
--- a/sa4029/bad/bad.go
+++ b/sa4029/bad/bad.go
@@ -1,6 +1,8 @@
 package main
+
 import "sort"
+
 func main() {
-    x := []string{"b", "a"}
-    x = sort.StringSlice(x)
+	x := []string{"b", "a"}
+	sort.Strings(x)
 }
--- a/sa4032/ok/ok.go
+++ b/sa4032/ok/ok.go
@@ -1,12 +1,13 @@
 //go:build !plan9
 
 package main
+
 import "runtime"
 
 const plan9 = "plan9"
 
 func main() {
-    _ = runtime.GOOS == "linux"
-    _ = "plan9" == runtime.GOOS
-    _ = runtime.GOOS == plan9
+	_ = runtime.GOOS == "linux"
+	_ = runtime.GOOS == "plan9"
+	_ = runtime.GOOS == plan9
 }
--- a/sa5004/bad/bad.go
+++ b/sa5004/bad/bad.go
@@ -6,7 +6,7 @@
 		select {
 		case i := <-ch:
 			_ = i
-		default:
+
 		}
 	}
 }
--- a/sa6005/bad/bad.go
+++ b/sa6005/bad/bad.go
@@ -2,10 +2,10 @@
 
 import "strings"
 
-func f(a, b string) bool { return strings.ToLower(a) == strings.ToLower(b) }
+func f(a, b string) bool { return strings.EqualFold(a, b) }
 
 // The `!=` spelling. Until 2026-08-27 the fixture held only `==`, so guff's
 // message for this one — and the negation its fix has to add — went unmeasured.
 // `ToLower`, not `ToUpper`: the unit-test harness stubs `strings` with the
 // functions the fixtures use, and only the operator matters here.
-func g(a, b string) bool { return strings.ToLower(a) != strings.ToLower(b) }
+func g(a, b string) bool { return !strings.EqualFold(a, b) }
--- a/sa9002/bad/bad.go
+++ b/sa9002/bad/bad.go
@@ -1,3 +1,5 @@
 package main
+
 import "os"
-func f() { os.Chmod("x", 644) }
+
+func f() { os.Chmod("x", 0644) }
--- a/sa9004/bad/bad.go
+++ b/sa9004/bad/bad.go
@@ -10,7 +10,7 @@
 // which are the ones that leave the group with a single member.
 const (
 	A E = 1
-	B   = 2
+	B E = 2
 )
 
 func main() {
