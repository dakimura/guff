# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh goimports
# Produced by running `golangci-lint run --fix` on compat/golden/cases/goimports/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/blanks/blanks.go
+++ b/blanks/blanks.go
@@ -5,7 +5,6 @@
 
 import (
 	"fmt"
-	"os"
 )
 
 var _ = fmt.Sprint
--- a/groups/groups.go
+++ b/groups/groups.go
@@ -14,6 +14,7 @@
 	"os"
 
 	"third.example/pkg"
+
 	"zlocal.example/local"
 )
 
