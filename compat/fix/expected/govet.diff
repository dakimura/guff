# Expected `--fix` result — generated, do not hand-edit.
#
# Regenerate with: ./compat/fix/regen.sh govet
# Produced by running `golangci-lint run --fix` on compat/golden/cases/govet/
# and diffing the materialized tree before -> after.
#
# An absent file for a case means upstream's --fix changes nothing there, and
# guff must change nothing either.
# golangci-lint: 2.12.2

--- a/assign/bad/bad.go
+++ b/assign/bad/bad.go
@@ -2,5 +2,4 @@
 
 func main() {
 	var x int
-	x = x
 }
--- a/composites/bad/bad.go
+++ b/composites/bad/bad.go
@@ -3,5 +3,5 @@
 import "example.com/govet/composites/other"
 
 func bad() {
-	_ = other.Config{nil}
+	_ = other.Config{Err: nil}
 }
--- a/hostport/bad/bad.go
+++ b/hostport/bad/bad.go
@@ -6,10 +6,10 @@
 )
 
 func direct(host string, port int) {
-	net.Dial("tcp", fmt.Sprintf("%s:%d", host, port))
+	net.Dial("tcp", net.JoinHostPort(host, fmt.Sprintf("%d", port)))
 }
 
 func viaVar(host, port string) {
-	addr := fmt.Sprintf("%s:%s", host, port)
+	addr := net.JoinHostPort(host, port)
 	net.Dial("tcp", addr)
 }
--- a/inline/bad/bad.go
+++ b/inline/bad/bad.go
@@ -3,5 +3,5 @@
 import "reflect"
 
 func usePtr(v reflect.Value) bool {
-	return v.Kind() == reflect.Ptr
+	return v.Kind() == reflect.Pointer
 }
--- a/inline_local/bad/bad.go
+++ b/inline_local/bad/bad.go
@@ -6,5 +6,5 @@
 const Legacy = Preferred
 
 func use() int {
-	return Legacy
+	return Preferred
 }
--- a/sigchanyzer/bad/bad.go
+++ b/sigchanyzer/bad/bad.go
@@ -6,6 +6,6 @@
 )
 
 func f() {
-	c := make(chan os.Signal)
+	c := make(chan os.Signal, 1)
 	signal.Notify(c, os.Interrupt)
 }
--- a/stringintconv/bad/bad.go
+++ b/stringintconv/bad/bad.go
@@ -1,5 +1,7 @@
 package main
 
+import "fmt"
+
 func main() {
-	_ = string(65)
+	_ = fmt.Sprint(rune(65))
 }
--- a/timeformat/bad/bad.go
+++ b/timeformat/bad/bad.go
@@ -3,5 +3,5 @@
 import "time"
 
 func f() {
-	_, _ = time.Parse("2006-02-01", "2020-01-15")
+	_, _ = time.Parse("2006-01-02", "2020-01-15")
 }
--- a/unreachable/bad/bad.go
+++ b/unreachable/bad/bad.go
@@ -2,5 +2,4 @@
 
 func bad() {
 	return
-	_ = 1
 }
