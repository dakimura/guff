# What guff's `--fix` writes TODAY for staticcheck-checks-not-s — generated, do not hand-edit.
#
# A pending baseline, not an allowlist: the case's real expectation is
# expected/staticcheck-checks-not-s.diff, and it is printed in full on every run. This file only
# keeps CI green while the gap is worked down, and it fails the gate if guff's
# output moves in *either* direction — including once guff gets it right, so the
# ledger cannot quietly outlive the defect it records.
#
# Re-record with: ./compat/fix/regen.sh --pending staticcheck-checks-not-s
--- a/checks/checks.go
+++ b/checks/checks.go
@@ -24,7 +24,7 @@
 
 // STCategory writes the comparison the wrong way round: ST1017.
 func STCategory(x int) bool {
-	if 1 == x {
+	if x == 1 {
 		return true
 	}
 	return false
@@ -32,11 +32,12 @@
 
 // QFCategory can have its `if/else if` chain rewritten: QF1003.
 func QFCategory(x int) string {
-	if x == 1 {
+	switch x {
+	case 1:
 		return "one"
-	} else if x == 2 {
+	case 2:
 		return "two"
-	} else if x == 3 {
+	case 3:
 		return "three"
 	}
 	return fmt.Sprint(x)
