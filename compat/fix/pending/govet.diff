# What guff's `--fix` writes TODAY for govet — generated, do not hand-edit.
#
# A pending baseline, not an allowlist: the case's real expectation is
# expected/govet.diff, and it is printed in full on every run. This file only
# keeps CI green while the gap is worked down, and it fails the gate if guff's
# output moves in *either* direction — including once guff gets it right, so the
# ledger cannot quietly outlive the defect it records.
#
# Re-record with: ./compat/fix/regen.sh --pending govet
--- a/assign/bad/bad.go
+++ b/assign/bad/bad.go
@@ -2,5 +2,4 @@
 
 func main() {
 	var x int
-	x = x
 }
--- a/composites/bad/bad.go
+++ b/composites/bad/bad.go
@@ -3,5 +3,5 @@
 import "example.com/govet/composites/other"
 
 func bad() {
-	_ = other.Config{nil}
+	_ = other.Config{Err: nil}
 }
--- a/sigchanyzer/bad/bad.go
+++ b/sigchanyzer/bad/bad.go
@@ -6,6 +6,6 @@
 )
 
 func f() {
-	c := make(chan os.Signal)
+	c := make(chan os.Signal, 1)
 	signal.Notify(c, os.Interrupt)
 }
--- a/stringintconv/bad/bad.go
+++ b/stringintconv/bad/bad.go
@@ -1,5 +1,7 @@
 package main
 
+import "fmt"
+
 func main() {
-	_ = string(65)
+	_ = fmt.Sprint(rune(65))
 }
--- a/timeformat/bad/bad.go
+++ b/timeformat/bad/bad.go
@@ -3,5 +3,5 @@
 import "time"
 
 func f() {
-	_, _ = time.Parse("2006-02-01", "2020-01-15")
+	_, _ = time.Parse("2006-01-02", "2020-01-15")
 }
--- a/unreachable/bad/bad.go
+++ b/unreachable/bad/bad.go
@@ -2,5 +2,4 @@
 
 func bad() {
 	return
-	_ = 1
 }
