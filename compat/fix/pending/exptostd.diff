# What guff's `--fix` writes TODAY for exptostd — generated, do not hand-edit.
#
# A pending baseline, not an allowlist: the case's real expectation is
# expected/exptostd.diff, and it is printed in full on every run. This file only
# keeps CI green while the gap is worked down, and it fails the gate if guff's
# output moves in *either* direction — including once guff gets it right, so the
# ledger cannot quietly outlive the defect it records.
#
# Re-record with: ./compat/fix/regen.sh --pending exptostd
--- a/bad.go
+++ b/bad.go
@@ -1,6 +1,6 @@
 package p
 
-import "golang.org/x/exp/maps"
+import "maps"
 
 func Bad(m map[string]string) {
 	_ = maps.Clone(m)
