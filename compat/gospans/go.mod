module guff.dev/compat/gospans

// Pinned to golangci-lint v2.12.2's own go directive, like the oracles: this
// program parses the same sources golangci-lint parses, so it must accept
// exactly the syntax that toolchain accepts. See ../oracles/README.md.
go 1.25.0
