# The fixture the isolate tier uses, so the two cannot drift.
bad/bad.go   compat/isolate/fixtures/wrapcheck/bad.go
# Func literals, `var` declarations, and the one package here whose path and
# name differ (`net/http`), which is what pins the signature's qualifier.
literal/literal.go   compat/isolate/fixtures/wrapcheck/literal.go
