# The isolate fixture, so the two tiers cannot drift.
bad.go            compat/isolate/fixtures/gomodguard/bad.go

# The stub module, replaced in so `./...` never lints it.
logrus/go.mod     compat/golden/cases/gomodguard/logrus.go.mod
logrus/logrus.go  compat/golden/cases/gomodguard/logrus.go.txt
