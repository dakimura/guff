// go.mod for the stub `gopkg.in/yaml.v3` module, materialized to
// `yamlv3/go.mod` by sources.txt. Scaffolding, not a fixture: G117 matches on
// import path plus function name, so the body is irrelevant.
module gopkg.in/yaml.v3

go 1.24
