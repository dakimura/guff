// go.mod for the stub `golang.org/x/crypto` module, materialized to
// `xcrypto/go.mod` by sources.txt. Scaffolding, not a fixture: the packages
// under it are the same stub sources the Rust unit tests use.
module golang.org/x/crypto

go 1.24
