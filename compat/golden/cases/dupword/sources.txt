# The isolate fixture, so the two tiers cannot drift.
bad.go      compat/isolate/fixtures/dupword/bad.go

# The stub module, replaced in so `./...` never lints it.
bad/bad.go  crates/guff-comment/tests/testdata/dupword/bad.go
